# Rails Coverage Batch 25

Date/time: 2026-07-18T03:22:23Z

## Scope

Rails coverage only. VP/Vitest coverage was intentionally not run.

## Coverage and test status

- Starting Rails line coverage: 45,064 / 49,351 (91.3132%).
- Starting Rails branch coverage: 10,331 / 14,649 (70.5236%).
- Ending Rails coverage: not measurable. The full report was invalidated by a pre-existing
  test-load/runtime failure before the suite completed; the resulting partial SimpleCov report was
  not used as an ending value.
- Starting known full-suite failures/errors: 0 / 0 in the preceding completed batch.
- Ending full-suite failures/errors: full suite did not complete. The first completed retry stopped
  while loading `SignWebauthn`; after test-only compatibility repairs, the focused legacy passkey
  slice reported 1 failure and 5 errors.

## Selected target

The target was the untested Side health endpoint matrix:

- `side/app` health, liveness, readiness, and startup controllers.
- `side/com` health, liveness, readiness, and startup controllers.
- `side/org` health, liveness, readiness, and startup controllers.

## Tests and changes

- Added the three Side surfaces to `test/integration/health_endpoints_test.rb`.
- Focused verification passed: 18 runs, 1,104 assertions, 0 failures, 0 errors, 0 skips.
- Updated stale test references from removed `SignWebauthn` and `Webauthn.trusted_origins` APIs to
  the current testable boundaries. These were test-only repairs needed to proceed past test loading;
  no application or database code was changed.
- Replaced the obsolete `Sign::WebauthnTest` contract with coverage for the current
  `PasskeyCeremonyContext` API.
- No dead code was deleted. No app/db changes were made.

## Commands run

- `bin/rails test test/integration/health_endpoints_test.rb`
- `bin/rails test test/controllers/concerns/sign/app_verification_base_included_do_test.rb test/controllers/concerns/sign/org_verification_base_included_do_test.rb test/controllers/auth/org/in/passkeys_controller_test.rb test/controllers/concerns/sign/webauthn_test.rb`
- `bundle exec rubocop -a`
- `COVERAGE=true bin/rails test test/` (attempted; interrupted once, then stopped on the existing
  `SignWebauthn`/passkey test failures)

## Blocker and skipped areas

The full Rails command cannot currently provide a trustworthy ending coverage value because the
existing authentication/passkey test slice is out of sync with the current WebAuthn implementation.
The remaining failures include obsolete `with_challenge` expectations and verifier mocks lacking the
current response contract. Auth/session/token/OIDC, payment, external integrations, routes,
fixtures, factories, configuration, and database changes were skipped.

## Next batch candidates

After the passkey test baseline is repaired in a separately scoped batch, rerun the full Rails
report, then continue with deterministic untested health/info controller branches and simple
serializers or validators. Do not count the partial report generated by the failed full run as a
coverage result.
