# 2026-09-17 E10 reverse-direction closeout

Work performed: classified remaining in-cycle slices; added fail-closed tests on shipped grant/admission/freshness/DPoP/OTP/logout/GUID paths; declared explicit `public` on `RailsPerformanceRecordSanitizer::RequestRecordPatch#save` and `RailsPerformanceStoreResilience::UtilsPatch#save_to_redis` so the architecture/RuboCop gate could run.

Focused command used `scripts/test-isolated` with `VALKEY_TEST_HOST=valkey`, logical DBs 3/4/5, `POSTGRESQL_TEST_HOST`.

Results:

- Focused twice: 182 runs, 951 assertions, 0 failures, 0 errors, 0 skips.
- E1 public subset: 94 runs, 417 assertions, 0 failures.
- Architecture baseline after visibility fix: 3 runs, 3,078 assertions, 0 failures.
- Ordinary isolated Rails (16 workers): 13,086 runs, 79,265 assertions, 0 failures, 0 errors, 3 skips.
- `COVERAGE=true` isolated `bin/rails test test/`: 13,086 runs, 79,320 assertions, 0 test failures/errors, 3 skips; SimpleCov exit 2.
  - Line 98.33% (58,001/58,985) — above floor 97.
  - Branch 87.83% (8,672/9,873) — below floor 90. Lowest 0% files include `lib/coverband_process_gate.rb` (process allowlist loaded before Rails; environment construction, not a Minitest subject per `adr/no-test-suite-for-environment-construction.md`), `lib/architecture_baseline.rb` (tooling), `lib/diagnostic_surface_credentials.rb` (diagnostic surface), `app/controllers/auth/app/social/sessions_controller.rb`, `app/controllers/concerns/regional_root_redirect.rb`.
  - Method 93.95% (10,082/10,731) — below floor 95. Lowest 0% files include Base org identity rotations and Edit org publishing help controllers (present production classes not entered by this suite; not verified dead).
  - Gates were not lowered, skipped, or excluded. The red SimpleCov result is an app/gate outcome, not an unavailable launcher.
- JS: `bun run test` 1,057 passed; `bun run test:coverage` statements 100%, branches 99.63%, functions 100%, lines 100%.
- Canonical `bin/ci` with explicit Valkey DBs 3/4/5 (not nested inside `test-isolated`): exit 0 in 7m27s. Rails stage 13,086 runs, 79,258 assertions, 0 failures, 0 errors, 3 skips. Style/security/JS coverage/server smoke passed. RuboCop passed after the visibility declarations.
- Brakeman 0 warnings. bundler-audit 0 vulnerabilities.

No GitHub write, deployment, live IdP/mail/SMS/Turnstile, or all-95 GO claim.
